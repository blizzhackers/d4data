/**
 * Definition: ValidComponents
 * Hash: d69c6f96
 */

#include "ValidComponents.h"

void ValidComponents::read(const char* base, char* &ptr) {
  char *current;
  current = ptr + 0x0;
  readData(&unk_38ba886, base, current);
  ptr += 0x10;
}
