/**
 * Definition: tbef5a4a
 * Hash: bef5a4a
 */

#include "tbef5a4a.h"

void tbef5a4a::read(const char* base, char* &ptr) {
  readData(&nX, base, ptr);
  readData(&nY, base, ptr);
  readData(&unk_2ec05dd, base, ptr);
}
