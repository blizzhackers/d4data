/**
 * Definition: t3ecfe93c
 * Hash: 3ecfe93c
 */

#include "t3ecfe93c.h"

void t3ecfe93c::read(const char* base, char* &ptr) {
  char *current;
  current = ptr + 0x0;
  readData(&arTiers, base, current);
  ptr += 0x10;
}
